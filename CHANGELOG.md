# سجلّ التغييرات

كل إصدار من «نون» مُوثَّق هنا. الأرقام على [الإصدار الدلالي](https://semver.org/lang/ar/):
الرقم الأوسط يزيد بإضافة ما لا يكسر البرامج القائمة، والأوّل بما يكسرها.

## [غير منشور]

### مكتبة «نصوص»

- وحدة ثانية في المكتبة القياسية، مكتوبة بـ«نون»، لما يلزم النصّ العربي خاصّة:
  `بلا_تشكيل`، و`وحد_الهمزات`، و`متشابهان` (بإهمال التشكيل والهمزات)، و`متناظر`،
  و`أرقام_لاتينية` و`أرقام_هندية`؛ وفحص المحارف `هجائي` و`عربي` و`رقمي` و`مسافة`؛
  و`أسطر` و`كلمات` و`عدد_الكلمات` و`عكس_الكلمات`؛ و`حشو_البداية` و`حشو_النهاية`
  و`توسيط` و`اقتطع`؛ و`مواضع` و`عدد_مرات` و`تكرار_المحارف`؛ و`قالب`.
- لها في محرّر الكتل صنف «مكتبة نصوص» بسبع كتل، ومثال «كلمات جملة».

### رسائل الأخطاء

- خطأ يقع داخل دالة من وحدة مستوردة يذكر الوحدة وسطره فيها:
  `خطأ تشغيل [سطر 20 في «رياضيات»]: …`. ويعرض سطر الأوامر بعده سطر الوحدة ثم
  سطر البرنامج الذي بدأ منه النداء، وتعلّم ساحة التجربة سطر البرنامج ذاك.
- `اخرج([رمز])` تُنهي البرنامج فورًا برمز خروج يراه سطر الأوامر، ولا تلتقطها `امسك`.
  والمترجِم المكتوب بـ«نون» يخرج بالرمز ١ عند الخطأ كالمترجِم الأصلي.

## [1.2.0] — 2026-09-22

«نون» تكتب نفسها: مُفسِّرها ومترجِمها بـ«نون»، ولها وحدات ومكتبة قياسية أولى،
ويُبرمَج بها بتركيب الكتل.

**قبل الترقية:** الدوال المدمجة الجديدة (`استورد`، `رمز`، `محرف`، `اقرأ_ملف`، `وسائط`، `عضو`،
`احفظ_مترجما`) ثوابت عامّة كغيرها، فبرنامج عرّف في مستواه الأعلى متغيّرًا بأحد
هذه الأسماء يحتاج إلى تسميته باسم آخر. المتغيّرات المحلّية داخل الدوال لا تتأثّر.

### محرّر الكتل

- صفحة جديدة بجوار ساحة التجربة، `playground/blocks.html`: برمجة «نون» بتركيب
  الكتل (Blockly، من اليمين إلى اليسار). الشروط والحلقات والمتغيّرات والدوال
  والنصوص والقوائم بعناوين من كلمات «نون»، وكتل خاصّة لـ`اطبع` و`اقرأ` ومكتبة
  `رياضيات`.
- الشيفرة الناتجة تظهر حيّة في لسان «الشيفرة»، وتعمل في المتصفّح بعامل الساحة نفسه؛
  وسطر الخطأ يُعلَّم فيها. «اكتبها نصًّا» تفتحها في المحرّر النصّي، و«مشاركة» تنسخ
  رابطًا بالكتل نفسها. ستّة أمثلة جاهزة، وفاتح وداكن، وتعمل على الهاتف.

### الوحدات والمكتبة القياسية

- `استورد("اسم")` تُحمِّل ملف «نون» وحدةً وتُرجعها، وتُقرأ تعريفاتها بالنقطة:
  `استورد("رياضيات").عاملي(٥)`. يُبحث عن الوحدة بجوار المستورِد ثم في المكتبة
  القياسية؛ وما بدأ بـ`_` لا يُصدَّر، وكل وحدة تُنفَّذ مرّة واحدة وتحتفظ بعوامّها،
  والاستيراد الدائري خطأ واضح.
- أوّل وحدة في المكتبة القياسية، مكتوبة بـ«نون»: **`رياضيات`** — العاملي والتوافيق
  والتباديل، القاسم والمضاعف المشترك، الجذر الصحيح، الأعداد الأولية وعواملها،
  فيبوناتشي، القوّة بباقٍ، والمتوسط والوسيط والمنوال والتباين والانحراف المعياري.
- المكتبة تُشحن مع الحزمة ومع ساحة التجربة.

### «نون بنون»

- `selfhost/noon.noon`: مُفسِّر «نون» مكتوب بـ«نون» نفسها، يطابق الآلة حرفًا بحرف
  على الأمثلة وبرامج الاختبار التفاضلي وكل مثال في التوثيق.
- ومترجِم «نون» مكتوب بـ«نون»: `noon selfhost/noon.noon --ترجم برنامج.noon`
  يكتب ملف `.noonc` تشغّله الآلة، مطابقًا لناتج المترجِم الأصلي تعليمةً تعليمة.
  وهو يترجم نفسه: المترجِم المُترجَم به يُخرج الملف نفسه بايتًا بايتًا.
- دوال مدمجة جديدة: `رمز` و`محرف` و`اقرأ_ملف` و`وسائط` و`عضو` و`احفظ_مترجما`.
- ما بعد اسم البرنامج يصل إليه بـ`وسائط()`: `noon برنامج.noon أ ب`.

## [1.1.0] — 2026-09-16

أوّل إصدار منشور.

### اللغة

- صيغة عربية كاملة: `متغير` و`ثابت`، و`إذا`/`وإلا`، و`طالما` و`لكل … في`،
  والدوال والإغلاق، والأصناف والوراثة و`الأصل`، و`حاول`/`امسك`/`أخيرًا` و`ارمِ`.
- الأرقام العربية-الهندية والفارسية واللاتينية سواء في الشيفرة، و`--عربي`
  للإخراج بها.
- الكلمات المفتاحية تتسامح في رسم الهمزة والألف المقصورة والتاء المربوطة، ولا
  تتسامح في التشكيل: `صنّف` اسم لا الكلمة `صنف`.
- ٢٨ دالة مدمجة والثابت `باي`، وتوابع للنصوص والقوائم والقواميس.
- رسائل أخطاء عربية برقم السطر، وسهم تحت موضع الخطأ النحوي.

### محرّكان بناتج واحد

- **المترجِم والآلة الافتراضية** (الافتراض): ترجمة إلى بايت-كود تنفّذه آلة
  مكدّسية، أسرع من المُفسِّر الشجري بنحو الضعف، وتبلغ عمق تعاود ٥٠٠٠.
- **المُفسِّر الشجري** (`--شجرة`): المرجع الذي تُقارَن به الآلة في كل اختبار وكل
  مثال في التوثيق.
- `--ترجم` يكتب ملف `.noonc` يُشغَّل دون ترجمة، و`--فكّك` يعرض البايت-كود
  بأسماء تعليمات عربية.

### الأدوات

- **ساحة التجربة**: محرّر في المتصفّح يشغّل «نون» نفسها عبر Pyodide، مع إيقاف
  البرامج التي لا تنتهي، وتمييز سطر الخطأ، ولوحة دخل لـ`اقرأ()`، وروابط مشاركة.
- **إضافة VS Code**: تلوين صيغة مولَّد من المُفسِّر، ومقاطع جاهزة، وحدود كلمة
  تفهم التشكيل.
- **صدفة تفاعلية** تُبقي التعريفات بين الأسطر.

### التوثيق

- موقع عربي كامل على GitHub Pages، كل مثال فيه يُشغَّل وقت البناء، وكل ناتج
  مكتوب فيه يُقارَن بالتشغيل الحقيقي في الاختبارات.

### إصلاحات قبل النشر

- `توقف` و`أرجع` خارج حلقة أو دالة كانتا تُسقطان المُفسِّر بخطأ Python، و`توقف`
  داخل دالة كانت تكسر حلقة المُنادي؛ صارت كلها أخطاء لغة.
- المساواة كانت تحوّل الأعداد إلى عشرية، فكان `٢ ** ٦٠ + ١ == ٢ ** ٦٠` صادقًا.

[1.2.0]: https://github.com/djouamaah/noon-lang/releases/tag/v1.2.0
[1.1.0]: https://github.com/djouamaah/noon-lang/releases/tag/v1.1.0
